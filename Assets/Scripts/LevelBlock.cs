using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LevelBlock : MonoBehaviour//clase que define cada bloque de nivel del juego
{
    public Transform startPoint, exitPoint;//declaramos los tipos de objetos transform publicos, para en el editor asignarles los gameObject referentes al startPoint y al endPoint
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
